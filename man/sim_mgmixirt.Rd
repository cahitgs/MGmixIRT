% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_mgmixirt}
\alias{sim_mgmixirt}
\title{Simulate data from a multigroup mixture performance decline model}
\usage{
sim_mgmixirt(
  model = c("2pdm", "hybrid", "mpdm"),
  n,
  I,
  a = NULL,
  b = NULL,
  switch_point = NULL,
  pdec = NULL,
  dshift = 1,
  pi_nd = NULL,
  omega = NULL,
  btilde = NULL,
  kappa = NULL,
  mu_nd = NULL,
  mu_dec = NULL,
  rho = NULL,
  sigma = NULL,
  seed = NULL
)
}
\arguments{
\item{model}{One of \code{"2pdm"}, \code{"hybrid"}, \code{"mpdm"}.}

\item{n}{Integer vector of group sample sizes.}

\item{I}{Number of items.}

\item{a, b}{Item slopes and intercepts (length \code{I}). Defaults:
slopes drawn from a log-normal distribution, intercepts from N(0, 1).}

\item{switch_point}{Switching point \code{i0} (2PDM only).}

\item{pdec}{Decline-class probability per group (2PDM only).}

\item{dshift}{Intercept decrement per group for items after the switching
point (2PDM only); scalar or vector recycled over groups, or an
\code{(I - i0) x G} matrix.}

\item{pi_nd}{No-decline class probability per group (HYBRID/MPDM).}

\item{omega}{Shape parameter per group (HYBRID/MPDM).}

\item{btilde}{Common aberrant-regime intercept per group (HYBRID).}

\item{kappa}{Decrement parameter per group (MPDM).}

\item{mu_nd}{No-decline class ability mean per group.}

\item{mu_dec}{Decline class ability mean per group (2PDM).}

\item{rho}{Slope of the class-mean function per group (HYBRID/MPDM).}

\item{sigma}{Ability standard deviation per group.}

\item{seed}{Optional RNG seed.}
}
\value{
A list with the response matrix \code{resp}, the group factor
  \code{group}, the true switching points \code{delta}, abilities
  \code{theta}, and the generating parameters \code{truth}.
}
\description{
Generates dichotomous item responses from the multigroup 2PDM, HYBRID,
or MPDM (List et al., 2017). Group sizes and group-specific structural
parameters are supplied as vectors with one element per group.
}
